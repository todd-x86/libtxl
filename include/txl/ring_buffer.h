#pragma once

#include "types.h"

namespace txl
{
    template<class Value>
    class ring_buffer
    {
    public:
        class const_iterator
        {
        };

        class iterator
        {
        };
    };
}
